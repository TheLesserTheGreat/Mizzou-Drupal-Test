/**
 * @file
 * Subtheme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.subtheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
